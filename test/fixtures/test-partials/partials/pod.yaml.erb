---
apiVersion: v1
kind: Pod
metadata:
  name: <%= locals['name'] %>
spec:
  <%= partial "restart-policy" %>
  activeDeadlineSeconds: 60
  containers:
  - name: <%= name %>
    image: busybox
    args: <%= args %>
  env:
    <%= partial 'env' %>
    - name: extra
      value: data
