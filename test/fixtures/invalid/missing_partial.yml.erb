apiVersion: extensions/v1beta1 # keep as long as this group is valid
kind: Deployment
metadata:
  name: bad-probe
  annotations:
    kubernetes-deploy.shopify.io/timeout-override: '25s'
spec:
  <%= partial 'missing-partial' %>
